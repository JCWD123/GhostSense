# 📂 BettaFish 生成报告文件位置说明

## 🎯 核心要点

**所有生成的报告都会自动保存到本地文件，即使网页刷新也不会丢失！**

---

## 📍 5000 端口（ReportEngine）报告位置

### 📁 保存目录

```
C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports\
```

### 📄 文件格式

```
final_report_{查询关键词}_{时间戳}.html
report_state_{查询关键词}_{时间戳}.json
```

### 🔍 示例

```
final_reports/
├── final_report_武汉大学舆情_20250827_131630.html    ← 📊 HTML格式的完整报告
└── report_state_武汉大学舆情_20250827_131630.json   ← 📦 JSON格式的状态文件
```

### ✅ 如何查看？

**方法 1：直接打开 HTML 文件**
1. 进入 `final_reports` 文件夹
2. 双击 `.html` 文件
3. 浏览器会自动打开显示完整报告

**方法 2：在文件管理器中查看**
```bash
# Windows
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports
dir

# 查看最新的报告
dir /O-D
```

**方法 3：通过 VS Code 打开**
- 在 VS Code 左侧文件树中找到 `final_reports` 文件夹
- 右键点击 `.html` 文件 → "在默认浏览器中打开"

---

## 🌐 Docker 部署的容器内外路径映射

### 卷挂载配置（docker-compose.yml）

```yaml
volumes:
  - ./logs:/app/logs
  - ./final_reports:/app/final_reports                          # ← 5000端口报告
  - ./insight_engine_streamlit_reports:/app/insight_engine_streamlit_reports  # ← 8501端口报告
  - ./media_engine_streamlit_reports:/app/media_engine_streamlit_reports      # ← 8502端口报告
  - ./query_engine_streamlit_reports:/app/query_engine_streamlit_reports      # ← 8503端口报告
```

### 路径对照表

| 容器内路径（Docker内部） | 宿主机路径（你的电脑） | 说明 |
|------------------------|-------------------|------|
| `/app/final_reports/` | `C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports\` | 5000端口生成的报告 |
| `/app/insight_engine_streamlit_reports/` | `C:\Users\HP\Desktop\MediaCrawer\BettaFish\insight_engine_streamlit_reports\` | 8501端口生成的报告 |
| `/app/media_engine_streamlit_reports/` | `C:\Users\HP\Desktop\MediaCrawer\BettaFish\media_engine_streamlit_reports\` | 8502端口生成的报告 |
| `/app/query_engine_streamlit_reports/` | `C:\Users\HP\Desktop\MediaCrawer\BettaFish\query_engine_streamlit_reports\` | 8503端口生成的报告 |

**💡 关键点：** 容器内生成的文件会实时同步到宿主机的对应目录！

---

## 📊 所有报告目录一览

### 📂 目录结构

```
BettaFish/
│
├── final_reports/                              # 5000端口 - ReportEngine
│   ├── final_report_武汉大学舆情_20250827_131630.html
│   └── report_state_武汉大学舆情_20250827_131630.json
│
├── insight_engine_streamlit_reports/          # 8501端口 - Insight Engine
│   ├── deep_search_report_武汉大学舆情_20250824_194529.md
│   ├── state_武汉大学舆情_20250824_194529.json
│   └── ...
│
├── media_engine_streamlit_reports/            # 8502端口 - Media Engine
│   ├── deep_search_report_武汉大学_20250825_175347.md
│   ├── state_武汉大学_20250825_175347.json
│   └── ...
│
└── query_engine_streamlit_reports/            # 8503端口 - Query Engine
    ├── deep_search_report_武汉大学舆情_20250825_104743.md
    ├── state_武汉大学舆情_20250825_104743.json
    └── ...
```

---

## 🔢 端口对应关系

| 端口 | 引擎 | 报告目录 | 文件格式 | 报告类型 |
|-----|------|---------|---------|---------|
| **5000** | **ReportEngine** | `final_reports/` | **HTML** | **多智能体协同生成的专业舆情报告** |
| 8501 | Insight Engine | `insight_engine_streamlit_reports/` | Markdown | 私有数据库深度分析报告 |
| 8502 | Media Engine | `media_engine_streamlit_reports/` | Markdown | 多模态内容分析报告 |
| 8503 | Query Engine | `query_engine_streamlit_reports/` | Markdown | 网页搜索分析报告 |

---

## 📋 文件命名规则

### 1. ReportEngine（5000端口）

**HTML 报告：**
```
final_report_{查询关键词}_{时间戳}.html

格式：final_report_{query_safe}_{YYYYMMDD_HHMMSS}.html
示例：final_report_武汉大学舆情_20250827_131630.html
```

**状态文件：**
```
report_state_{查询关键词}_{时间戳}.json

格式：report_state_{query_safe}_{YYYYMMDD_HHMMSS}.json
示例：report_state_武汉大学舆情_20250827_131630.json
```

### 2. Streamlit引擎（8501/8502/8503端口）

**Markdown 报告：**
```
deep_search_report_{查询关键词}_{时间戳}.md

格式：deep_search_report_{query_safe}_{YYYYMMDD_HHMMSS}.md
示例：deep_search_report_武汉大学舆情_20250824_194529.md
```

**状态文件：**
```
state_{查询关键词}_{时间戳}.json

格式：state_{query_safe}_{YYYYMMDD_HHMMSS}.json
示例：state_武汉大学舆情_20250824_194529.json
```

---

## 🔍 查找报告的方法

### 方法 1：按时间排序查看最新报告

#### Windows PowerShell
```powershell
# 进入报告目录
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports

# 按修改时间倒序排列（最新的在最上面）
Get-ChildItem -File | Sort-Object LastWriteTime -Descending
```

#### Windows CMD
```cmd
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports
dir /O-D
```

#### Linux/macOS
```bash
cd ~/path/to/BettaFish/final_reports
ls -lt
```

---

### 方法 2：按关键词搜索

#### Windows PowerShell
```powershell
# 搜索包含"武汉大学"的报告
Get-ChildItem -Filter "*武汉大学*"

# 搜索今天生成的报告
Get-ChildItem -Filter "*20250827*"
```

#### Windows CMD
```cmd
dir "*武汉大学*"
dir "*20250827*"
```

#### Linux/macOS
```bash
# 搜索包含"武汉大学"的报告
ls -lh | grep "武汉大学"

# 搜索今天生成的报告
ls -lh | grep "20250827"
```

---

### 方法 3：使用 VS Code

1. **打开文件树**
   - 在 VS Code 左侧文件树中找到对应的报告目录
   - 点击文件夹右侧的刷新按钮查看最新文件

2. **搜索文件**
   - 按 `Ctrl + P`（Windows）或 `Cmd + P`（macOS）
   - 输入关键词，如 `final_report`
   - 列表会显示所有匹配的文件

3. **在文件管理器中打开**
   - 右键点击 `final_reports` 文件夹
   - 选择 "在文件资源管理器中显示"
   - 直接在系统文件管理器中查看

---

## 🎨 报告文件内容说明

### HTML 报告（5000端口）

**内容包括：**
- ✅ 执行摘要
- ✅ 舆情态势分析
- ✅ 情感分析可视化
- ✅ 媒体报道分析
- ✅ 社会影响评估
- ✅ 应对建议
- ✅ 结论与展望
- ✅ 交互式图表
- ✅ 数据来源标注

**特点：**
- 📊 包含交互式图表和可视化
- 🎨 专业的页面布局和样式
- 📱 响应式设计，支持移动端查看
- 🔗 可以直接用浏览器打开

---

### JSON 状态文件

**内容包括：**
```json
{
  "query": "武汉大学舆情",
  "start_time": "2025-08-27T13:16:30",
  "end_time": "2025-08-27T13:20:45",
  "status": "completed",
  "template_used": "企业品牌声誉分析报告模板",
  "forum_logs": [...],
  "metadata": {...}
}
```

**用途：**
- 🔄 记录生成过程的完整状态
- 🔍 用于调试和复现
- 📊 分析生成时间和性能
- 🔧 支持断点续传（如果中途中断可以继续）

---

## ⚡ 快速访问命令

### 创建快捷方式（Windows）

#### 方法 A：创建桌面快捷方式

```powershell
# 在 PowerShell 中运行
$WshShell = New-Object -ComObject WScript.Shell
$Shortcut = $WshShell.CreateShortcut("$env:USERPROFILE\Desktop\BettaFish报告.lnk")
$Shortcut.TargetPath = "C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports"
$Shortcut.Save()
```

#### 方法 B：添加到快速访问

1. 打开文件资源管理器
2. 导航到 `C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports`
3. 右键点击地址栏的文件夹图标
4. 选择"固定到快速访问"

---

### 创建别名（Linux/macOS）

```bash
# 在 ~/.bashrc 或 ~/.zshrc 中添加
alias reports='cd ~/path/to/BettaFish/final_reports && ls -lht'

# 重新加载配置
source ~/.bashrc  # 或 source ~/.zshrc

# 使用
reports
```

---

## 🔄 报告自动备份建议

### 方法 1：Windows 定时备份脚本

创建 `backup_reports.bat`：

```batch
@echo off
set SOURCE=C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports
set BACKUP=C:\Users\HP\Desktop\报告备份\%date:~0,10%

mkdir "%BACKUP%" 2>nul
xcopy "%SOURCE%\*.html" "%BACKUP%\" /Y /I
xcopy "%SOURCE%\*.json" "%BACKUP%\" /Y /I

echo 备份完成: %BACKUP%
pause
```

**添加到任务计划程序：**
1. 打开"任务计划程序"
2. 创建基本任务
3. 设置触发器（如每天一次）
4. 操作：启动程序，选择 `backup_reports.bat`

---

### 方法 2：使用 Git 版本控制

```bash
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports

# 初始化 git 仓库
git init

# 添加 .gitignore（可选，排除大文件）
echo "*.html filter=lfs diff=lfs merge=lfs -text" > .gitignore

# 提交当前报告
git add .
git commit -m "备份报告 - $(date)"
```

---

### 方法 3：云同步（推荐）

**将报告目录设置为云同步文件夹：**
- 📦 OneDrive
- 💾 Google Drive
- 🌐 坚果云
- 🔒 百度网盘

**优点：**
- ✅ 自动备份
- ✅ 跨设备访问
- ✅ 版本历史
- ✅ 误删恢复

---

## 🐛 常见问题

### ❓ 问题 1：找不到报告文件

**可能原因：**
1. Docker 容器没有正确启动
2. 卷挂载配置错误
3. 报告生成失败

**解决方法：**

```bash
# 1. 检查 Docker 容器是否运行
docker ps

# 2. 检查容器日志
docker logs bettafish

# 3. 检查卷挂载
docker inspect bettafish | grep -A 10 "Mounts"

# 4. 进入容器查看
docker exec -it bettafish ls -lh /app/final_reports/
```

---

### ❓ 问题 2：报告生成了但找不到

**检查步骤：**

```bash
# Windows PowerShell
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish

# 搜索所有 HTML 文件
Get-ChildItem -Recurse -Filter "*.html" -File | Select-Object FullName, LastWriteTime

# 搜索最近 1 小时内生成的文件
Get-ChildItem -Recurse -Filter "*.html" -File | Where-Object {$_.LastWriteTime -gt (Get-Date).AddHours(-1)}
```

---

### ❓ 问题 3：HTML 文件打不开或显示不正常

**原因：**
- 文件生成不完整
- 浏览器缓存问题

**解决方法：**

```bash
# 1. 检查文件大小（正常应该大于 10KB）
Get-ChildItem final_reports/*.html | Select-Object Name, Length

# 2. 清除浏览器缓存
# - Chrome: Ctrl + Shift + Delete
# - Edge: Ctrl + Shift + Delete
# - Firefox: Ctrl + Shift + Delete

# 3. 尝试用不同浏览器打开
# - Chrome
# - Edge
# - Firefox
```

---

### ❓ 问题 4：想找回之前删除的报告

**如果有备份：**
1. 检查回收站
2. 检查云同步的版本历史
3. 检查 Docker 容器内是否还有

**从容器内恢复：**

```bash
# 列出容器内的文件
docker exec bettafish ls -lh /app/final_reports/

# 从容器复制到宿主机
docker cp bettafish:/app/final_reports/final_report_武汉大学_20250827_131630.html C:\Users\HP\Desktop\
```

---

## 📊 统计报告数量

### PowerShell

```powershell
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish

# 统计各类报告数量
Write-Host "5000端口报告数量:" (Get-ChildItem final_reports/*.html).Count
Write-Host "8501端口报告数量:" (Get-ChildItem insight_engine_streamlit_reports/*.md).Count
Write-Host "8502端口报告数量:" (Get-ChildItem media_engine_streamlit_reports/*.md).Count
Write-Host "8503端口报告数量:" (Get-ChildItem query_engine_streamlit_reports/*.md).Count

# 计算总大小
$totalSize = (Get-ChildItem -Recurse -Include *.html,*.md | Measure-Object -Property Length -Sum).Sum
Write-Host "报告总大小:" ([math]::Round($totalSize/1MB, 2)) "MB"
```

---

## 🎯 总结

### ✅ 记住这几点

1. **5000端口的报告在这里：**
   ```
   C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports\
   ```

2. **文件名格式：**
   ```
   final_report_{关键词}_{日期时间}.html
   ```

3. **网页刷新不会丢失报告：**
   - 所有报告都会自动保存到本地文件
   - Docker 容器内的文件会实时同步到宿主机

4. **快速查找最新报告：**
   ```powershell
   Get-ChildItem C:\Users\HP\Desktop\MediaCrawer\BettaFish\final_reports -File | Sort-Object LastWriteTime -Descending | Select-Object -First 5
   ```

5. **直接双击 HTML 文件就可以在浏览器中查看！**

---

**希望这份文档能帮助你快速找到生成的舆情报告！** 📊🎉

有任何问题欢迎随时询问！




