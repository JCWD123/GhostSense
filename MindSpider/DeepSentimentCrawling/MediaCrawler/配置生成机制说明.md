# 📋 BettaFish 配置生成机制说明

## 🎯 核心问题

**问：在 `base_config.py` 中手动修改 `KEYWORDS` 是否会被覆盖？**

**答：是的，会被覆盖！** 

---

## 🔍 配置生成机制详解

### 1. 配置覆盖的工作流程

```
用户启动 MindSpider
    ↓
run_complete_workflow() 运行完整工作流
    ↓
【第一步】BroadTopicExtraction 模块
    - 从新闻源（RSS/API）获取热点新闻
    - 使用 AI 提取关键词
    - 保存到数据库 daily_topics 表
    ↓
【第二步】DeepSentimentCrawling 模块
    - KeywordManager 从数据库获取最新关键词
    - PlatformCrawler.create_base_config() 重写配置文件
    ↓
⚠️ base_config.py 被重写！
    - KEYWORDS 被替换为数据库中的关键词
    - PLATFORM、CRAWLER_TYPE 等也被重写
    ↓
MediaCrawler 使用新配置进行爬取
```

### 2. 配置覆盖的核心函数

**位置：** `MindSpider/DeepSentimentCrawling/platform_crawler.py`

```python
def create_base_config(self, platform: str, keywords: List[str], 
                      crawler_type: str = "search", max_notes: int = 50) -> bool:
    """
    创建MediaCrawler的基础配置
    ⚠️ 此函数会重写 base_config.py 文件！
    """
    # 将关键词列表转换为逗号分隔的字符串
    keywords_str = ",".join(keywords)
    
    # 读取原始配置文件
    with open(base_config_path, 'r', encoding='utf-8') as f:
        content = f.read()
    
    # 逐行修改配置
    for line in lines:
        if line.startswith('PLATFORM = '):
            new_lines.append(f'PLATFORM = "{platform}"')
        elif line.startswith('KEYWORDS = '):
            # 🔥 关键词被覆盖！
            new_lines.append(f'KEYWORDS = "{keywords_str}"')
        elif line.startswith('CRAWLER_TYPE = '):
            new_lines.append(f'CRAWLER_TYPE = "{crawler_type}"')
        # ... 其他配置项
    
    # 🚨 写入新配置，覆盖原文件
    with open(base_config_path, 'w', encoding='utf-8') as f:
        f.write('\n'.join(new_lines))
```

### 3. 关键词来源

**位置：** `MindSpider/DeepSentimentCrawling/keyword_manager.py`

```python
def get_latest_keywords(self, target_date: date = None, max_keywords: int = 100) -> List[str]:
    """
    从数据库获取最新的关键词列表
    优先级：当天关键词 > 最近7天关键词 > 默认关键词
    """
    # 1. 尝试获取当天的关键词
    topics_data = self.get_daily_topics(target_date)
    if topics_data and topics_data.get('keywords'):
        return topics_data['keywords']
    
    # 2. 如果没有，获取最近7天的关键词
    recent_topics = self.get_recent_topics(days=7)
    if recent_topics:
        return merged_keywords
    
    # 3. 都没有，返回默认关键词
    return self._get_default_keywords()
```

---

## ⚠️ 为什么会被覆盖？

### 设计目标

BettaFish 的设计目标是**自动化舆情监控**：

1. 每天自动提取热点话题
2. 自动生成爬取关键词
3. 无需人工干预，自动运行

### 覆盖的原因

```python
# 在 platform_crawler.py 的 run_crawler() 方法中
def run_crawler(self, platform: str, keywords: List[str], ...):
    # 🔥 每次运行爬虫前都会调用此函数
    if not self.create_base_config(platform, keywords, "search", max_notes):
        return {"success": False, "error": "基础配置创建失败"}
    
    # 然后才执行爬取
    cmd = [sys.executable, "main.py", "--platform", platform, ...]
```

**结论：** 
- `create_base_config()` 在**每次爬取前**都会被调用
- 它会**强制重写** `base_config.py` 文件
- 所以手动修改的内容会被覆盖

---

## ✅ 如何正确使用自定义关键词？

### 方案 1：直接运行 MediaCrawler（推荐）

**绕过 MindSpider 的自动化流程，直接使用 MediaCrawler**

```bash
# 1. 进入 MediaCrawler 目录
cd MindSpider/DeepSentimentCrawling/MediaCrawler

# 2. 手动修改配置文件
vim config/base_config.py

# 修改为你想要的关键词
KEYWORDS = "你的关键词1,你的关键词2,你的关键词3"

# 3. 直接运行 MediaCrawler（不经过 MindSpider）
python main.py --platform xhs --lt qrcode --type search
```

**优点：**
- ✅ 配置不会被覆盖
- ✅ 完全自定义关键词
- ✅ 适合测试和临时爬取

**缺点：**
- ❌ 需要手动运行
- ❌ 不使用 MindSpider 的自动化功能

---

### 方案 2：修改数据库中的关键词

**直接在数据库中插入自定义关键词**

```sql
-- 1. 连接到 MindSpider 数据库
mysql -u root -p

USE bettafish;  -- 或你的数据库名

-- 2. 插入自定义关键词到 daily_topics 表
INSERT INTO daily_topics (
    extract_date,
    keywords,
    summary,
    created_at,
    updated_at
) VALUES (
    '2025-11-24',  -- 今天的日期
    '["你的关键词1", "你的关键词2", "你的关键词3"]',  -- JSON 格式的关键词列表
    '手动插入的自定义关键词',
    NOW(),
    NOW()
);

-- 3. 查看是否插入成功
SELECT * FROM daily_topics WHERE extract_date = '2025-11-24';
```

**然后运行 MindSpider：**

```bash
# MindSpider 会自动从数据库读取你插入的关键词
python main.py --deep-sentiment --date 2025-11-24
```

**优点：**
- ✅ 使用 MindSpider 的自动化流程
- ✅ 关键词持久化保存
- ✅ 可以批量管理关键词

**缺点：**
- ❌ 需要手动操作数据库
- ❌ 需要了解数据库结构

---

### 方案 3：使用命令行参数（未来支持）

**通过命令行直接指定关键词**

```bash
# 这是理想的使用方式（需要修改代码）
python main.py --deep-sentiment \
  --platform xhs \
  --keywords "关键词1,关键词2,关键词3" \
  --max-notes 100
```

**实现方式：**

修改 `MindSpider/DeepSentimentCrawling/main.py`：

```python
def run_platform_crawling(self, platform: str, 
                         keywords: List[str] = None,  # 👈 新增参数
                         target_date: date = None,
                         max_keywords: int = 50, 
                         max_notes: int = 50,
                         login_type: str = "qrcode") -> Dict:
    
    # 如果指定了关键词，使用指定的；否则从数据库获取
    if keywords:
        print(f"📝 使用命令行指定的 {len(keywords)} 个关键词")
    else:
        keywords = self.keyword_manager.get_keywords_for_platform(
            platform, target_date, max_keywords
        )
    
    # 执行爬取
    result = self.platform_crawler.run_crawler(
        platform, keywords, login_type, max_notes
    )
```

**优点：**
- ✅ 灵活方便
- ✅ 不需要修改配置文件
- ✅ 适合命令行使用

**缺点：**
- ❌ 需要修改代码
- ❌ 目前尚未实现

---

### 方案 4：创建自定义关键词配置文件

**使用独立的关键词配置文件**

```bash
# 1. 创建自定义关键词文件
cat > custom_keywords.txt << EOF
你的关键词1
你的关键词2
你的关键词3
EOF

# 2. 修改 KeywordManager，支持从文件读取
# 在 keyword_manager.py 中添加：

def get_keywords_from_file(self, file_path: str) -> List[str]:
    """从文件读取关键词"""
    with open(file_path, 'r', encoding='utf-8') as f:
        keywords = [line.strip() for line in f if line.strip()]
    return keywords

# 3. 在 main.py 中添加参数
parser.add_argument('--keywords-file', type=str, help='关键词文件路径')

# 4. 使用
python main.py --deep-sentiment \
  --platform xhs \
  --keywords-file custom_keywords.txt
```

**优点：**
- ✅ 关键词管理方便
- ✅ 可以版本控制
- ✅ 易于批量修改

**缺点：**
- ❌ 需要修改代码
- ❌ 需要额外的文件管理

---

## 📊 配置文件对比

### 被自动覆盖的配置项

| 配置项 | 是否覆盖 | 覆盖来源 |
|--------|---------|---------|
| `PLATFORM` | ✅ 是 | 命令行参数 |
| `KEYWORDS` | ✅ 是 | 数据库 `daily_topics` |
| `CRAWLER_TYPE` | ✅ 是 | 固定为 `search` |
| `SAVE_DATA_OPTION` | ✅ 是 | 从 MindSpider 配置读取 |
| `CRAWLER_MAX_NOTES_COUNT` | ✅ 是 | 命令行参数 `max_notes` |
| `ENABLE_GET_COMMENTS` | ✅ 是 | 固定为 `True` |
| `HEADLESS` | ✅ 是 | 固定为 `True` |

### 不会被覆盖的配置项

| 配置项 | 是否覆盖 | 说明 |
|--------|---------|------|
| `ENABLE_IP_PROXY` | ❌ 否 | 保留原值 |
| `IP_PROXY_POOL_COUNT` | ❌ 否 | 保留原值 |
| `SAVE_LOGIN_STATE` | ❌ 否 | 保留原值 |
| `CRAWLER_MAX_SLEEP_SEC` | ❌ 否 | 保留原值 |
| 其他自定义配置 | ❌ 否 | 保留原值 |

**结论：** 只有核心爬取参数会被覆盖，代理、登录、速率等配置保持不变。

---

## 🔧 实战示例

### 场景 1：我想爬取特定关键词的小红书数据

**不推荐：** 修改 `base_config.py`（会被覆盖）

**推荐：**

```bash
# 方式 A：直接使用 MediaCrawler
cd MindSpider/DeepSentimentCrawling/MediaCrawler
vim config/base_config.py  # 修改 KEYWORDS
python main.py --platform xhs --lt qrcode --type search

# 方式 B：通过数据库插入
mysql -u root -p -e "
USE bettafish;
INSERT INTO daily_topics (extract_date, keywords, summary, created_at, updated_at)
VALUES ('2025-11-24', '[\"美食\", \"旅游\", \"摄影\"]', '手动测试', NOW(), NOW());
"
cd MindSpider
python main.py --deep-sentiment --platform xhs --date 2025-11-24
```

---

### 场景 2：我想每天自动爬取热点话题

**推荐：** 使用 MindSpider 完整工作流（不要手动修改配置）

```bash
# 完整工作流：自动提取话题 → 自动爬取
python main.py --complete

# 或使用定时任务
crontab -e
0 8 * * * cd /path/to/MindSpider && python main.py --complete
```

---

### 场景 3：我想测试爬虫功能，不想被覆盖

**推荐：** 创建独立的测试配置文件

```bash
# 1. 备份原配置
cd MindSpider/DeepSentimentCrawling/MediaCrawler/config
cp base_config.py base_config.backup.py

# 2. 创建测试配置
cp base_config.py test_config.py
vim test_config.py  # 修改为测试关键词

# 3. 修改代码，支持加载不同配置
# 在 MediaCrawler/main.py 中添加：
parser.add_argument('--config', default='base_config', help='配置文件名')

# 4. 使用测试配置运行
python main.py --platform xhs --config test_config
```

---

## 💡 总结与建议

### ⚠️ 核心要点

1. **`base_config.py` 中的 `KEYWORDS` 会被自动覆盖**
   - 覆盖时机：每次运行 MindSpider 的 DeepSentimentCrawling 模块前
   - 覆盖来源：数据库 `daily_topics` 表中的关键词

2. **覆盖函数：** `PlatformCrawler.create_base_config()`
   - 位置：`MindSpider/DeepSentimentCrawling/platform_crawler.py:139`
   - 调用时机：`run_crawler()` 方法中

3. **关键词来源：** `KeywordManager.get_latest_keywords()`
   - 位置：`MindSpider/DeepSentimentCrawling/keyword_manager.py:60`
   - 数据来源：`daily_topics` 表

### ✅ 最佳实践

| 使用场景 | 推荐方案 | 优先级 |
|---------|---------|--------|
| 自动化舆情监控 | 使用 MindSpider 完整工作流 | ⭐⭐⭐⭐⭐ |
| 临时测试/调试 | 直接运行 MediaCrawler | ⭐⭐⭐⭐ |
| 自定义关键词爬取 | 修改数据库 | ⭐⭐⭐ |
| 批量任务管理 | 数据库 + 脚本 | ⭐⭐⭐ |
| 开发新功能 | 创建独立配置文件 | ⭐⭐ |

### 🚀 推荐的工作方式

**方式 A：自动化运行（推荐给生产环境）**

```bash
# 让 MindSpider 全自动运行
python main.py --complete
# 不要手动修改 base_config.py
```

**方式 B：手动控制（推荐给开发/测试）**

```bash
# 直接使用 MediaCrawler，完全自定义
cd MindSpider/DeepSentimentCrawling/MediaCrawler
# 修改 config/base_config.py
python main.py --platform xhs --type search
```

---

## 📝 相关文件清单

| 文件路径 | 作用 | 是否被修改 |
|---------|-----|-----------|
| `MindSpider/DeepSentimentCrawling/MediaCrawler/config/base_config.py` | 爬虫主配置文件 | ✅ 会被覆盖 |
| `MindSpider/DeepSentimentCrawling/platform_crawler.py` | 平台爬虫管理器 | ❌ 不会被修改 |
| `MindSpider/DeepSentimentCrawling/keyword_manager.py` | 关键词管理器 | ❌ 不会被修改 |
| `数据库: daily_topics 表` | 关键词数据源 | ✅ 运行时读取 |

---

**希望这份文档能帮助你理解 BettaFish 的配置生成机制！** 🎉

如有疑问，请参考：
- Issue 讨论
- 项目 README
- 源代码注释




