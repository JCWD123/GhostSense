# ====================== 数据库配置 ======================
# BettaFish 统一数据库配置
# 支持 MySQL、PostgreSQL、MongoDB 三种数据库

# 数据库主机，例如localhost 或 127.0.0.1
DB_HOST=localhost

# 数据库端口号
# MySQL: 3306 | PostgreSQL: 5432 | MongoDB: 27017
DB_PORT=27017

# 数据库用户名
DB_USER=root

# 数据库密码
DB_PASSWORD=MyPassw0rd!

# 数据库名称
DB_NAME=xhs

# 数据库字符集，推荐utf8mb4，兼容emoji（仅SQL数据库使用）
DB_CHARSET=utf8mb4

# 🔥 数据库类型：mysql | postgresql | mongodb
# 只需修改这一个参数即可切换数据库！
DB_DIALECT=mongodb


# ====================== MongoDB 专用配置（可选）======================
# 如果需要 MongoDB 使用不同的配置，可以取消下面的注释
# 专用配置优先级高于通用配置

# MONGODB_HOST=localhost
# MONGODB_PORT=27017
# MONGODB_USER=mongouser
# MONGODB_PASSWORD=mongopass
# MONGODB_DB_NAME=bettafish
# MONGODB_AUTH_SOURCE=admin


# ====================== 使用说明 ======================
# 
# 1. MySQL 配置：
#    DB_DIALECT=mysql
#    DB_PORT=3306
#    使用命令：--save_data_option db
#
# 2. PostgreSQL 配置：
#    DB_DIALECT=postgresql
#    DB_PORT=5432
#    使用命令：--save_data_option postgresql
#
# 3. MongoDB 配置：
#    DB_DIALECT=mongodb
#    DB_PORT=27017
#    使用命令：--save_data_option mongodb
#
# 快速启动 MongoDB:
# docker run -d --name mongodb -p 27017:27017 \
#   -e MONGO_INITDB_ROOT_USERNAME=root \
#   -e MONGO_INITDB_ROOT_PASSWORD=MyPassw0rd! \
#   mongo:7.0
#
# 初始化 MongoDB:
# python main.py --init_db mongodb
#
# 使用 MongoDB 爬取:
# python main.py --platform xhs --type search --save_data_option mongodb




