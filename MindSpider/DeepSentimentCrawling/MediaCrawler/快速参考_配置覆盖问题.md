# ⚠️ 配置覆盖问题 - 快速参考

## 核心问题

**Q: 为什么我修改了 `base_config.py` 中的 `KEYWORDS`，运行后又变回去了？**

**A: 因为 MindSpider 每次运行前会自动重写这个文件！**

---

## 🔍 原因

```
MindSpider 启动
    ↓
从数据库读取热点关键词
    ↓
调用 create_base_config() 函数
    ↓
🔥 重写 base_config.py 文件
    ↓
你的修改被覆盖！
```

**覆盖的配置项：**
- ✅ `KEYWORDS` - 从数据库读取
- ✅ `PLATFORM` - 从命令行参数读取
- ✅ `CRAWLER_TYPE` - 固定为 `search`
- ✅ `HEADLESS` - 固定为 `True`

**不会覆盖的配置项：**
- ❌ `ENABLE_IP_PROXY` - 保留你的设置
- ❌ `SAVE_LOGIN_STATE` - 保留你的设置
- ❌ `CRAWLER_MAX_SLEEP_SEC` - 保留你的设置

---

## ✅ 解决方案

### 方案 1：直接使用 MediaCrawler（推荐）

```bash
# 1. 进入 MediaCrawler 目录
cd MindSpider/DeepSentimentCrawling/MediaCrawler

# 2. 修改配置文件
vim config/base_config.py
# 改为：KEYWORDS = "你的关键词1,你的关键词2"

# 3. 直接运行（不经过 MindSpider）
python main.py --platform xhs --lt qrcode --type search
```

**✅ 优点：配置不会被覆盖**

---

### 方案 2：修改数据库

```sql
-- 在数据库中插入自定义关键词
INSERT INTO daily_topics (extract_date, keywords, summary, created_at, updated_at)
VALUES (
    '2025-11-24',
    '["关键词1", "关键词2", "关键词3"]',
    '手动插入',
    NOW(),
    NOW()
);
```

```bash
# 然后运行 MindSpider
python main.py --deep-sentiment --date 2025-11-24
```

**✅ 优点：使用 MindSpider 的自动化流程**

---

### 方案 3：备份并恢复配置

```bash
# 备份你的配置
cp config/base_config.py config/my_config.py

# 运行 MindSpider 后恢复
cp config/my_config.py config/base_config.py

# 然后直接运行 MediaCrawler
python main.py --platform xhs --type search
```

---

## 📋 快速决策表

| 你的需求 | 推荐方案 |
|---------|---------|
| 自动爬取热点话题 | 使用 MindSpider，不要手动改配置 |
| 爬取特定关键词 | 直接运行 MediaCrawler |
| 测试爬虫功能 | 直接运行 MediaCrawler |
| 批量爬取任务 | 修改数据库，使用 MindSpider |

---

## 🔧 代码位置

**覆盖配置的函数：**
```
MindSpider/DeepSentimentCrawling/platform_crawler.py
    第 139 行：create_base_config()
```

**关键词来源：**
```
MindSpider/DeepSentimentCrawling/keyword_manager.py
    第 60 行：get_latest_keywords()
```

**调用时机：**
```
MindSpider/DeepSentimentCrawling/platform_crawler.py
    第 235 行：run_crawler() 中调用 create_base_config()
```

---

## 💡 记住这一点

**如果你使用 MindSpider 的自动化流程：**
```bash
python main.py --complete
python main.py --deep-sentiment
```

**那么 `base_config.py` 会被自动覆盖！**

**如果你想自定义关键词，请直接使用 MediaCrawler：**
```bash
cd MindSpider/DeepSentimentCrawling/MediaCrawler
python main.py --platform xhs --type search
```

---

## 📚 详细说明

查看完整文档：`配置生成机制说明.md`




