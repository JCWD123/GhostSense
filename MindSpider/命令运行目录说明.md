# 📍 BettaFish 命令运行目录说明

## 🎯 核心要点

BettaFish 项目有**两个不同的** `main.py` 文件，运行目录不同！

---

## 📂 项目目录结构

```
BettaFish/
├── MindSpider/                          # 🔥 主目录
│   ├── main.py                          # ✅ MindSpider 主程序
│   ├── config.py                        # 配置文件
│   ├── BroadTopicExtraction/           # 话题提取模块
│   │   └── main.py                      # 话题提取入口
│   └── DeepSentimentCrawling/          # 情感爬取模块
│       ├── main.py                      # 深度爬取入口
│       └── MediaCrawler/               # 🔥 MediaCrawler 目录
│           ├── main.py                  # ✅ MediaCrawler 主程序
│           ├── config/
│           │   └── base_config.py       # MediaCrawler 配置
│           └── media_platform/
```

---

## ⚡ 运行目录对照表

| 命令特征 | 运行目录 | 说明 |
|---------|---------|-----|
| `--deep-sentiment` | **`MindSpider/`** | MindSpider 主程序 |
| `--broad-topic` | **`MindSpider/`** | MindSpider 主程序 |
| `--complete` | **`MindSpider/`** | MindSpider 主程序 |
| `--platforms` | **`MindSpider/`** | MindSpider 主程序 |
| `--platform` (单数) | **`MindSpider/DeepSentimentCrawling/MediaCrawler/`** | MediaCrawler 主程序 |
| `--lt qrcode` | **`MindSpider/DeepSentimentCrawling/MediaCrawler/`** | MediaCrawler 主程序 |
| `--type search` | **`MindSpider/DeepSentimentCrawling/MediaCrawler/`** | MediaCrawler 主程序 |
| `--save_data_option` | **`MindSpider/DeepSentimentCrawling/MediaCrawler/`** | MediaCrawler 主程序 |

---

## 🔍 你的命令

```bash
python main.py --deep-sentiment --platforms xhs --test
```

### ✅ 正确的运行方式

```bash
# Windows
cd C:\Users\HP\Desktop\MediaCrawer\BettaFish\MindSpider
python main.py --deep-sentiment --platforms xhs --test

# Linux/macOS
cd /path/to/BettaFish/MindSpider
python main.py --deep-sentiment --platforms xhs --test
```

### 📋 命令参数解释

| 参数 | 含义 |
|-----|------|
| `--deep-sentiment` | 运行深度情感爬取模块（DeepSentimentCrawling） |
| `--platforms xhs` | 指定平台为小红书（可以指定多个：`xhs dy bili`） |
| `--test` | 测试模式（少量数据，用于测试和登录） |

---

## 🎨 常用命令示例

### 1️⃣ MindSpider 命令（在 `MindSpider/` 目录运行）

#### 完整工作流程

```bash
cd MindSpider

# 运行完整流程（话题提取 + 多平台爬取）
python main.py --complete

# 测试模式
python main.py --complete --test
```

#### 只运行话题提取

```bash
cd MindSpider

# 提取今天的热点话题
python main.py --broad-topic

# 提取指定日期的话题
python main.py --broad-topic --date 2024-01-15

# 提取更多关键词
python main.py --broad-topic --keywords-count 200
```

#### 只运行深度爬取

```bash
cd MindSpider

# 爬取所有平台（使用数据库中的关键词）
python main.py --deep-sentiment

# 测试模式（少量数据）
python main.py --deep-sentiment --test

# 指定单个平台
python main.py --deep-sentiment --platforms xhs --test

# 指定多个平台
python main.py --deep-sentiment --platforms xhs dy bili --test

# 指定日期
python main.py --deep-sentiment --date 2024-01-15

# 调整爬取数量
python main.py --deep-sentiment --max-keywords 30 --max-notes 20
```

#### 项目管理

```bash
cd MindSpider

# 查看项目状态
python main.py --status

# 初始化数据库
python main.py --init-db
```

---

### 2️⃣ MediaCrawler 命令（在 `MindSpider/DeepSentimentCrawling/MediaCrawler/` 目录运行）

#### 直接爬取（不使用 MindSpider 的自动化）

```bash
cd MindSpider/DeepSentimentCrawling/MediaCrawler

# 基本爬取
python main.py --platform xhs --lt qrcode --type search

# 指定关键词
python main.py --platform xhs --lt qrcode --type search --keywords "美食,旅游,摄影"

# 指定存储方式
python main.py --platform xhs --lt qrcode --type search --save_data_option db

# 爬取指定笔记
python main.py --platform xhs --lt qrcode --type detail

# 爬取创作者主页
python main.py --platform xhs --lt qrcode --type creator
```

#### 初始化数据库

```bash
cd MindSpider/DeepSentimentCrawling/MediaCrawler

# 初始化 MySQL
python main.py --init_db mysql

# 初始化 PostgreSQL
python main.py --init_db postgresql

# 初始化 MongoDB
python main.py --init_db mongodb
```

---

## 🚨 常见错误

### ❌ 错误 1：在错误的目录运行

```bash
# ❌ 错误：在 BettaFish 根目录运行
cd BettaFish
python main.py --deep-sentiment --platforms xhs --test
# 错误：没有找到 main.py
```

**正确做法：**

```bash
# ✅ 正确：进入 MindSpider 目录
cd BettaFish/MindSpider
python main.py --deep-sentiment --platforms xhs --test
```

---

### ❌ 错误 2：混淆两个程序的参数

```bash
# ❌ 错误：在 MindSpider 使用 MediaCrawler 的参数
cd MindSpider
python main.py --platform xhs --lt qrcode --type search
# 错误：MindSpider 不支持 --lt 参数
```

**正确做法：**

```bash
# ✅ 方式 A：使用 MindSpider 的参数
cd MindSpider
python main.py --deep-sentiment --platforms xhs --test

# ✅ 方式 B：使用 MediaCrawler
cd MindSpider/DeepSentimentCrawling/MediaCrawler
python main.py --platform xhs --lt qrcode --type search
```

---

### ❌ 错误 3：参数拼写错误

```bash
# ❌ 错误：--platform（单数）在 MindSpider
cd MindSpider
python main.py --deep-sentiment --platform xhs --test
# 错误：MindSpider 使用 --platforms（复数）
```

**正确做法：**

```bash
# ✅ 正确：使用 --platforms（复数）
cd MindSpider
python main.py --deep-sentiment --platforms xhs --test
```

---

## 📋 参数对照表

### MindSpider 参数（`MindSpider/main.py`）

| 参数 | 类型 | 说明 | 示例 |
|-----|------|-----|------|
| `--broad-topic` | 标志 | 运行话题提取模块 | `--broad-topic` |
| `--deep-sentiment` | 标志 | 运行深度爬取模块 | `--deep-sentiment` |
| `--complete` | 标志 | 运行完整工作流程 | `--complete` |
| `--platforms` | 多值 | 指定爬取平台（复数） | `--platforms xhs dy` |
| `--date` | 字符串 | 指定日期 | `--date 2024-01-15` |
| `--keywords-count` | 整数 | 话题提取的关键词数量 | `--keywords-count 200` |
| `--max-keywords` | 整数 | 每个平台最大关键词数 | `--max-keywords 50` |
| `--max-notes` | 整数 | 每个关键词最大爬取数 | `--max-notes 50` |
| `--test` | 标志 | 测试模式 | `--test` |
| `--status` | 标志 | 显示项目状态 | `--status` |
| `--init-db` | 标志 | 初始化数据库 | `--init-db` |

### MediaCrawler 参数（`MediaCrawler/main.py`）

| 参数 | 类型 | 说明 | 示例 |
|-----|------|-----|------|
| `--platform` | 枚举 | 指定平台（单数） | `--platform xhs` |
| `--lt` | 枚举 | 登录方式 | `--lt qrcode` |
| `--type` | 枚举 | 爬取类型 | `--type search` |
| `--keywords` | 字符串 | 关键词列表 | `--keywords "美食,旅游"` |
| `--save_data_option` | 枚举 | 存储方式 | `--save_data_option db` |
| `--get_comment` | 字符串 | 是否爬取评论 | `--get_comment yes` |
| `--init_db` | 枚举 | 初始化数据库 | `--init_db mysql` |
| `--start` | 整数 | 起始页码 | `--start 1` |

---

## 🎯 快速决策表

**我想要：** → **应该在哪个目录运行？**

| 目标 | 目录 | 命令 |
|-----|------|-----|
| 自动提取热点并爬取 | `MindSpider/` | `python main.py --complete` |
| 只提取热点话题 | `MindSpider/` | `python main.py --broad-topic` |
| 爬取所有平台 | `MindSpider/` | `python main.py --deep-sentiment` |
| 测试小红书登录 | `MindSpider/` | `python main.py --deep-sentiment --platforms xhs --test` |
| 爬取特定平台（多个） | `MindSpider/` | `python main.py --deep-sentiment --platforms xhs dy` |
| 自定义关键词爬取 | `MediaCrawler/` | `python main.py --platform xhs --lt qrcode --type search` |
| 直接操作 MediaCrawler | `MediaCrawler/` | `python main.py --platform xhs ...` |
| 初始化数据库 | `MindSpider/` 或 `MediaCrawler/` | `python main.py --init-db` |

---

## 💡 记忆技巧

### 🔑 关键规则

1. **有 `--deep-sentiment` / `--broad-topic` / `--complete`？**
   - → 在 `MindSpider/` 目录运行

2. **有 `--lt qrcode` / `--type search` / `--save_data_option`？**
   - → 在 `MediaCrawler/` 目录运行

3. **`--platforms`（复数）？**
   - → MindSpider（支持多平台同时爬取）

4. **`--platform`（单数）？**
   - → MediaCrawler（单平台爬取）

---

## 🎉 总结

### 你的命令

```bash
python main.py --deep-sentiment --platforms xhs --test
```

**应该在：**

```bash
cd MindSpider
python main.py --deep-sentiment --platforms xhs --test
```

**含义：**
- 运行 MindSpider 的深度情感爬取模块
- 只爬取小红书平台
- 使用测试模式（少量数据，用于测试登录）

**预期行为：**
1. 从数据库读取关键词
2. 配置 MediaCrawler 的 `base_config.py`
3. 调用 MediaCrawler 爬取小红书
4. 弹出二维码供扫码登录
5. 爬取少量数据（测试模式）
6. 实时保存到数据库

---

**祝你使用愉快！** 🚀




